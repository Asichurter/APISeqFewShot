# from torch.nn.utils.rnn import pad_packed_sequence, pack_padded_sequence
#
# class EpisodeData:
#     def __init__(self, support, query, sup_len, que_len):
#         self.Support = support
#         self.Query = query
#         self.SupportLen = sup_len
#         self.QueryLen = que_len
#
#     def update(self):

