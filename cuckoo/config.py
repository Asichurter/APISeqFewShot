import os
import json

from utils.manager import PathManager
from utils.file import loadJson, dumpJson

# class Manager:
#
#     def __init__(self, data_path, config_save_path):
#         cfg = loadJson(config_save_path)